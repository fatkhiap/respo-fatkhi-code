from tkinter import *

root = Tk()

one = Label(root, text="satu", bg="red", fg="yellow")
one.pack()
two = Label(root, text="dua", bg="green", fg="blue")
two.pack(fill=X)
three = Label(root, text="tiga", bg="purple", fg="white")
three.pack(side=RIGHT, fill=Y)

root.mainloop()
